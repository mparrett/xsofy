name: Deploy to GitHub Pages

on:
  push:
    branches: [experiment/mobile]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Install let-go (from mparrett fork, play branch)
        run: go install github.com/mparrett/let-go@play

      - name: Build WASM
        run: let-go -w dist main.lg

      - name: Stage shell.html for the bottom-bar host
        run: cp dev/shell.html dist/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
